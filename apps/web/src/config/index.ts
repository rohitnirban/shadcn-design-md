const config = {
  websiteName: "shadcn DESIGN.md | Export any preset",
  websiteUrl:
    process.env.NEXT_PUBLIC_WEBSITE_URL || "https://localhost:3000",
  websiteDescription:
    "Chrome extension that exports any shadcn preset on ui.shadcn.com/create as a portable DESIGN.md, raw decoded JSON, or resolved globals.css.",
};

export default config;
